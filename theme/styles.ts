const theme = {
  styles: {
            global: {                  
              '*, *::before, *::after': {
                        boxSizing: 'borderBox',
                        padding: 0,
                        margin: 0
              },
              "body": {
                    backgroundColor: "baseBackground"
              }
        }
  },
}

export default theme;